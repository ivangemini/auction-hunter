# GitHub Copilot instructions

Follow the repository-root `AGENTS.md` as the canonical engineering and product instruction set. Before editing a file, also follow any nearer nested `AGENTS.md` in its directory hierarchy.

Use the source-of-truth documents under `docs/` rather than inventing new architecture, economy, save or Yandex SDK conventions.
