# Gemini instructions

`AGENTS.md` is the canonical project instruction file. Read it before making changes, then read the nearest nested `AGENTS.md` for the target directory.

Do not duplicate project policy here. Consult the relevant source-of-truth document under `docs/` for architecture, economy, content, analytics and Yandex integration work.
