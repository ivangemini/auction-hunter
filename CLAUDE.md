# Claude instructions

`AGENTS.md` is the canonical project instruction file. Read it before making changes, then read the nearest nested `AGENTS.md` for the target directory.

Do not duplicate project policy in this file. Also consult the relevant source-of-truth document under `docs/` when changing architecture, economy, content, analytics or Yandex integration.
